httpserver
==========

Small http server on node &amp; go

INSTRUCTIONS

Make sure you’re running a mongo instance (type mongod if required).

git clone git@github.com:lkalneus/httpserver.git httpserver
cd httpserver
npm install

node server.js